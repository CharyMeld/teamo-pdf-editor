# Ai

**Responsibility:** AI-assisted document features (summarization, Q&A, etc.),
served exclusively by a local Ollama instance. Client/company documents must
never be sent to an external AI service — see ARCHITECTURE.md and the project's
standing security rules.

**Owns:** No tables yet.

**Phase 0 status:** Scaffolded directory only, no logic. No AI provider wired up.
