# Forms

**Responsibility:** PDF form field detection, filling, and data extraction.

**Owns:** No tables yet — no schema is justified until this phase is scoped
(field definitions, submitted values, etc. will need their own migration then).

**Phase 0 status:** Scaffolded directory only, no logic, no schema.
