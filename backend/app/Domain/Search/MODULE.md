# Search

**Responsibility:** Full-text and metadata search across a user's documents
(depends on OCR text layers and document metadata).

**Owns:** No tables yet — an index/store choice (DB full-text vs. dedicated search
engine) is deferred until this phase is scoped.

**Phase 0 status:** Scaffolded directory only, no logic.
