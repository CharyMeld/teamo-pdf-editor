# Signatures

**Responsibility:** Digital/electronic signature placement and application.

**Owns:** No tables yet — schema (signature fields, signer identity, signed
timestamp/hash) is deferred until this phase is scoped.

**Phase 0 status:** Scaffolded directory only, no logic, no schema.
