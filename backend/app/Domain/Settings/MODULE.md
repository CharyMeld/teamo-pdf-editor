# Settings

**Responsibility:** Application-wide key/value configuration that needs to be
editable at runtime (not just `.env`).

**Owns:** `settings` table (`App\Models\Setting`).

**Phase 0 status:** Schema + model only. No settings are defined or read yet —
added as features that need runtime configuration ship.
